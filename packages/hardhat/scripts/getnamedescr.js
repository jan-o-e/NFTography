 // Selecting the input element and get its value retrieving particularly those with the following id's name and description
 function getName() {
    let inputVal = document.getElementById("name").value;
    // Displaying the value
  return {reader};

  function getDesc() {
    let inputVal = document.getElementById("desc").value;
    // Displaying the value
  return {reader};

  export{getName, getDesc};
  